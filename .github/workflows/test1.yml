name: 'Migrated: test1'
"on":
  push:
    branches:
      - main
  workflow_dispatch: {}
jobs:
  migrated:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
